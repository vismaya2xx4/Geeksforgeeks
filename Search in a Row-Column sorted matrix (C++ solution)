class Solution {
    
  public:
    bool matSearch(vector<vector<int>> &arr, int x) {
        
    for(int i = 0 ; i < arr.size() ;i++)  {  
      if(std::binary_search(arr[i].begin(), arr[i].end(), x)){
          return 1;
      }
    }
    return 0;
    }
};
